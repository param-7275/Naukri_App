<%= render partial: 'partials/header' %>
<%= render partial: 'partials/errors' %>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t('jobs.edit.title') %></title>

  <style>
    :root {
      --primary: #882727;
      --primary-light: #a13b3b;
      --bg-light: #f5f7fa;
      --text-dark: #222;
      --text-light: #555;
      --white: #fff;
    }

    body {
      background-color: var(--bg-light);
      font-family: "Inter", "Segoe UI", Roboto, sans-serif;
      color: var(--text-dark);
      margin: 0;
      padding: 2rem;
    }

    .form-container {
      max-width: 750px;
      background: var(--white);
      margin: 2rem auto;
      padding: 2.5rem 2rem;
      border-radius: 14px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
    }

    .form-container:hover {
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }

    .form-title {
      text-align: center;
      font-size: 2rem;
      color: var(--primary);
      font-weight: 700;
      margin-bottom: 1.8rem;
      position: relative;
    }

    .form-title::after {
      content: '';
      position: absolute;
      bottom: -10px;
      left: 50%;
      transform: translateX(-50%);
      width: 80px;
      height: 3px;
      background-color: var(--primary);
      border-radius: 3px;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-label {
      display: block;
      font-weight: 600;
      color: var(--text-dark);
      margin-bottom: 8px;
      font-size: 0.95rem;
    }

    .form-input, .form-textarea, .trix-content {
      width: 100%;
      padding: 12px 14px;
      font-size: 1rem;
      border-radius: 8px;
      border: 1px solid #ccc;
      background: #fff;
      transition: all 0.2s ease;
    }

    .form-input:focus, .form-textarea:focus, .trix-content:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(136, 39, 39, 0.15);
      outline: none;
    }

    .form-textarea {
      height: 150px;
      resize: vertical;
    }

    .form-submit {
      width: 100%;
      background-color: var(--primary);
      color: #fff;
      border: none;
      padding: 14px;
      border-radius: 8px;
      font-size: 1.05rem;
      font-weight: 600;
      letter-spacing: 0.3px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .form-submit:hover {
      background-color: var(--primary-light);
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(136, 39, 39, 0.25);
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 1.2rem;
      text-decoration: none;
      color: var(--primary);
      font-weight: 600;
      font-size: 0.95rem;
      transition: color 0.3s ease;
    }

    .back-link:hover {
      color: var(--primary-light);
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .form-container {
        padding: 1.8rem 1.5rem;
      }

      .form-title {
        font-size: 1.6rem;
      }

      .form-input, .form-textarea {
        font-size: 0.95rem;
      }

      .form-submit {
        font-size: 1rem;
      }
    }
  </style>
</head>

<body>
  <%= form_with(model: @job, url: update_job_path(@job.id, locale: I18n.locale), method: :patch, html: { class: 'form-container' }) do |form| %>
    <h2 class="form-title"><%= t('jobs.edit.title') %></h2>

    <div class="form-group">
      <%= form.label :title, t('jobs.common.placeholders.title'), class: 'form-label' %>
      <%= form.text_field :title, class: 'form-input', placeholder: t('jobs.common.placeholders.title') %>
    </div>

    <div class="form-group">
      <%= form.label :description, t('jobs.common.placeholders.description'), class: 'form-label' %>
      <%= form.text_area :description, class: 'form-textarea', placeholder: t('jobs.common.placeholders.description') %>
    </div>

    <div class="form-group">
      <%= form.label :company_name, t('jobs.common.placeholders.company_name'), class: 'form-label' %>
      <%= form.text_field :company_name, class: 'form-input', placeholder: t('jobs.common.placeholders.company_name') %>
    </div>

    <div class="form-group">
      <%= form.label :vacancy, t('jobs.common.placeholders.vacancy'), class: 'form-label' %>
      <%= form.number_field :vacancy, class: 'form-input', placeholder: t('jobs.common.placeholders.vacancy') %>
    </div>

    <div class="form-group">
      <%= form.label :location, t('jobs.common.placeholders.location'), class: 'form-label' %>
      <%= form.text_field :location, class: 'form-input', placeholder: t('jobs.common.placeholders.location') %>
    </div>

    <div class="form-group">
      <%= form.label :industry_type, t('jobs.common.placeholders.industry_type'), class: 'form-label' %>
      <%= form.text_field :industry_type, class: 'form-input', placeholder: t('jobs.common.placeholders.industry_type') %>
    </div>

    <div class="form-group">
      <%= form.label :experience_range, t('jobs.common.placeholders.experience_range'), class: 'form-label' %>
      <%= form.text_field :experience_range, class: 'form-input', placeholder: t('jobs.common.placeholders.experience_range'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :salary, t('jobs.common.placeholders.salary'), class: 'form-label' %>
      <%= form.text_field :salary, class: 'form-input', placeholder: t('jobs.common.placeholders.salary') %>
    </div>

    <div class="form-group">
      <%= form.label :work_mode, t('jobs.common.placeholders.work_mode'), class: 'form-label' %>
      <%= form.text_field :work_mode, class: 'form-input', placeholder: t('jobs.common.placeholders.work_mode'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :employment_type, t('jobs.common.placeholders.employment_type'), class: 'form-label' %>
      <%= form.text_field :employment_type, class: 'form-input', placeholder: t('jobs.common.placeholders.employment_type'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :role_category, t('jobs.common.placeholders.role_category'), class: 'form-label' %>
      <%= form.text_field :role_category, class: 'form-input', placeholder: t('jobs.common.placeholders.role_category'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :education, t('jobs.common.placeholders.education'), class: 'form-label' %>
      <%= form.text_field :education, class: 'form-input', placeholder: t('jobs.common.placeholders.education'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :skills, t('jobs.common.placeholders.skills'), class: 'form-label' %>
      <%= form.text_area :skills, class: 'form-input', placeholder: t('jobs.common.placeholders.skills'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :company_description, t('jobs.common.placeholders.company_description'), class: 'form-label' %>
      <%= form.text_area :company_description, class: 'form-input', placeholder: t('jobs.common.placeholders.company_description'), required: true %>
    </div>

    <%= form.submit t('jobs.edit.submit'), class: 'form-submit' %>
    <%= link_to t('jobs.edit.back'), job_description_path(job_id: @job.id), class: 'back-link' %>
  <% end %>
</body>
</html>

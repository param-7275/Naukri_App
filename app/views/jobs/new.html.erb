<%= render partial: 'partials/header' %>
<%= render partial: 'partials/errors' %>

<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= t('jobs.new.title') %></title>

  <style>
    :root {
      --primary: #882727;
      --primary-light: #a13b3b;
      --bg-light: #f5f7fa;
      --text-dark: #222;
      --text-light: #555;
      --white: #fff;
    }

    body {
      background-color: var(--bg-light);
      font-family: "Inter", "Segoe UI", Roboto, sans-serif;
      color: var(--text-dark);
      margin: 0;
      padding: 2rem;
    }

    .form-container {
      max-width: 750px;
      background: var(--white);
      margin: 2rem auto;
      padding: 2.5rem 2rem;
      border-radius: 14px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      transition: all 0.3s ease;
    }

    .form-container:hover {
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
    }

    .form-title {
      text-align: center;
      font-size: 2rem;
      color: var(--primary);
      font-weight: 700;
      margin-bottom: 1.8rem;
      position: relative;
    }

    .form-title::after {
      content: '';
      position: absolute;
      bottom: -10px;
      left: 50%;
      transform: translateX(-50%);
      width: 80px;
      height: 3px;
      background-color: var(--primary);
      border-radius: 3px;
    }

    .form-group {
      margin-bottom: 1.5rem;
    }

    .form-label {
      display: block;
      font-weight: 600;
      color: var(--text-dark);
      margin-bottom: 8px;
      font-size: 0.95rem;
    }

    .form-input, .form-textarea {
      width: 100%;
      padding: 12px 14px;
      font-size: 1rem;
      border-radius: 8px;
      border: 1px solid #ccc;
      background: #fff;
      transition: all 0.2s ease;
    }

    .form-input:focus, .form-textarea:focus {
      border-color: var(--primary);
      box-shadow: 0 0 0 3px rgba(136, 39, 39, 0.15);
      outline: none;
    }

    .form-textarea {
      height: 150px;
      resize: vertical;
    }

    .form-submit {
      width: 100%;
      background-color: var(--primary);
      color: #fff;
      border: none;
      padding: 14px;
      border-radius: 8px;
      font-size: 1.05rem;
      font-weight: 600;
      letter-spacing: 0.3px;
      cursor: pointer;
      transition: all 0.3s ease;
    }

    .form-submit:hover {
      background-color: var(--primary-light);
      transform: translateY(-2px);
      box-shadow: 0 4px 10px rgba(136, 39, 39, 0.25);
    }

    .back-link {
      display: block;
      text-align: center;
      margin-top: 1.2rem;
      text-decoration: none;
      color: var(--primary);
      font-weight: 600;
      font-size: 0.95rem;
      transition: color 0.3s ease;
    }

    .back-link:hover {
      color: var(--primary-light);
      text-decoration: underline;
    }

    @media (max-width: 768px) {
      .form-container {
        padding: 1.8rem 1.5rem;
      }

      .form-title {
        font-size: 1.6rem;
      }

      .form-input, .form-textarea {
        font-size: 0.95rem;
      }

      .form-submit {
        font-size: 1rem;
      }
    }
  </style>
</head>

<body>
  <%= form_with(model: @job, url: create_new_job_path, html: { class: 'form-container' }) do |form| %>
    <h2 class="form-title"><%= t('jobs.new.title') %></h2>

    <div class="form-group">
      <%= form.label :title, t('jobs.common.placeholders.title'), class: 'form-label' %>
      <%= form.text_field :title, class: 'form-input', placeholder: t('jobs.common.placeholders.title'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :description, t('jobs.common.placeholders.description'), class: 'form-label' %>
      <%= form.text_area :description, class: 'form-textarea', placeholder: t('jobs.common.placeholders.description'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :company_name, t('jobs.common.placeholders.company_name'), class: 'form-label' %>
      <%= form.text_field :company_name, class: 'form-input', placeholder: t('jobs.common.placeholders.company_name'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :vacancy, t('jobs.common.placeholders.vacancy'), class: 'form-label' %>
      <%= form.number_field :vacancy, class: 'form-input', placeholder: t('jobs.common.placeholders.vacancy'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :location, t('jobs.common.placeholders.location'), class: 'form-label' %>
      <%= form.text_field :location, class: 'form-input', placeholder: t('jobs.common.placeholders.location'), required: true %>
    </div>

    <div class="form-group">
      <%= form.label :industry_type, t('jobs.common.placeholders.industry_type'), class: 'form-label' %>
      <%= form.text_field :industry_type, class: 'form-input', placeholder: t('jobs.common.placeholders.industry_type'), required: true %>
    </div>

    <%= form.submit t('jobs.new.submit'), class: 'form-submit' %>
    <%= link_to t('jobs.edit.back'), recruiter_home_path, class: 'back-link' %>

  <% end %>
</body>
</html>
